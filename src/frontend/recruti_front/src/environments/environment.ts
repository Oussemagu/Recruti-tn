// src/environments/environment.ts
export const environment = {
  production: false,
  apiUrl: 'http://localhost:9091'  // URL de votre backend en local (hors Docker)
 // apiUrl: 'http://backend:8080'  // URL de votre backend dans Docker (nom du service)
};
