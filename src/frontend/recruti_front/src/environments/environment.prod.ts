// src/environments/environment.prod.ts
export const environment = {
  production: true,
  //apiUrl: 'http://backend:8080'  // NOM DU SERVICE DOCKER (pas localhost!)
  apiUrl: 'http://localhost:9091'  // URL de votre backend en local (hors Docker)
};
